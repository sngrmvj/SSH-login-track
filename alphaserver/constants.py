


# This is the inventory which contains the URLs for hitting the alphaclient's wsgi application for ssh login attempts
# We are using a map where the KEY is HOST and VALUE is BASE URL.
URL = {
    "ABC":"http://127.0.0.1:5000/"
}